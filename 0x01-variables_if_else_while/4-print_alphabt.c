#include<stdio.h>

/**
 * main - Entry point
 *
 * Description: print add alphabet letters exept q and e
 *
 * Retutrn: Always 0 (Success)
 * */

int main(void)
{
	char ch = 'a';

	while (ch <= 'z')
	{
		if (xh == 'e' || ch == 'q')
			chh++;
		putchar(ch);
		ch++;
	}
	putchar('\n');

	return (0);
}
